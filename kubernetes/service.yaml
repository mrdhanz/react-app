apiVersion: v1
kind: Service
metadata:
  name: react-app
  namespace: default
spec:
  selector:
    app: react-app
  ports:
    - protocol: TCP
      port: 8089
      targetPort: 8089
  type: LoadBalancer
